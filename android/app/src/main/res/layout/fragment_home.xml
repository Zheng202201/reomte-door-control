<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:orientation="vertical"
    android:paddingStart="@dimen/home_padding"
    android:paddingTop="6dp"
    android:paddingEnd="@dimen/home_padding"
    android:paddingBottom="6dp">

    <include
        android:id="@+id/includeVideoStatus"
        layout="@layout/include_video_status" />

    <include
        android:id="@+id/includeVideoPanel"
        layout="@layout/include_video_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <include
        android:id="@+id/includeDoorControls"
        layout="@layout/include_door_controls" />
</LinearLayout>
